<UserControl x:Class="JuanLog.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:JuanLog.Views"
             xmlns:viewmodels="clr-namespace:JuanLog.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:LoginViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.Background>
            <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                <GradientStop Color="#FF154852" Offset="0.997"/>
                <GradientStop Color="#FF9DCFD8" Offset="0"/>
                <GradientStop Color="#FE4ECAE2" Offset="0.515"/>
            </LinearGradientBrush>

        </Grid.Background>

        <TextBlock HorizontalAlignment="Center" Height="60" Margin="0,70,0,0" TextWrapping="Wrap" Text="Přihlášení" VerticalAlignment="Top" Width="236" FontSize="48" FontFamily="Constantia" TextDecorations="Underline" FontWeight="Bold"/>

        <TextBox Name="UsernameBox" HorizontalAlignment="Center" Height="19" Margin="0,217,0,0" TextWrapping="Wrap" Text="Juan" VerticalAlignment="Top" Width="200"/>
        <PasswordBox Name="PasswordBox" HorizontalAlignment="Center" Height="19" Margin="0,273,0,0" VerticalAlignment="Top" Width="200" Password=""/>
        <Button Content="Přihlásit" HorizontalAlignment="Left" Margin="402,335,0,0" VerticalAlignment="Top" Click="LogInToVM" Height="20" Width="116" />
        <Button Content="Nemáš účet? Zaregistruj se!" HorizontalAlignment="Left" Height="20" Margin="230,335,0,0" VerticalAlignment="Top" Width="167" Command="{Binding ToRegistrationCommandCommand, Mode=OneWay}" />


    </Grid>
</UserControl>
