<UserControl x:Class="JuanLog.Views.HomepageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:JuanLog.Views"
             xmlns:viewmodels="clr-namespace:JuanLog.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:HomepageViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.Background>
            <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                <GradientStop Color="#FF154852" Offset="0.997"/>
                <GradientStop Color="#FF9DCFD8" Offset="0"/>
                <GradientStop Color="#FE4ECAE2" Offset="0.515"/>
            </LinearGradientBrush>
        </Grid.Background>

        <TextBlock HorizontalAlignment="Left" Height="90" Margin="274,71,0,0" TextWrapping="Wrap" Text="{Binding Greeter}" VerticalAlignment="Top" Width="365" FontSize="16"/>
        <Button Content="Importovat data ze souboru" HorizontalAlignment="Left" Margin="33,187,0,0" VerticalAlignment="Top" Height="25" Command="{Binding ToImportCommand}"/>
        <Button Content="Přidat cvičení" HorizontalAlignment="Left" Margin="33,241,0,0" VerticalAlignment="Top" Width="152" Height="25" Command="{Binding ToAddExerciseCommand}"/>
        <Button Content="Podívat se na svůj progress" HorizontalAlignment="Left" Margin="33,291,0,0" VerticalAlignment="Top" Width="152" Height="25" Command="{Binding ToProgressCommand}"/>
        <Button Content="Můj profil" HorizontalAlignment="Left" Margin="33,341,0,0" VerticalAlignment="Top" Width="152" Height="25" Command="{Binding ToProfileCommand}"/>
        <Button Content="Odhlásit" HorizontalAlignment="Left" Margin="33,391,0,0" VerticalAlignment="Top" Width="152" Height="25" Command="{Binding ToLoginCommand}"/>




    </Grid>
</UserControl>
