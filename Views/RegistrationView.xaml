<UserControl x:Class="JuanLog.Views.RegistrationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:JuanLog.Views"
             xmlns:viewmodels="clr-namespace:JuanLog.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:RegistrationViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.Background>
            <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                <GradientStop Color="#FF154852" Offset="0.997"/>
                <GradientStop Color="#FF9DCFD8" Offset="0"/>
                <GradientStop Color="#FE4ECAE2" Offset="0.515"/>
            </LinearGradientBrush>
        </Grid.Background>


        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>


        <TextBlock Grid.Column="1" Grid.Row="1" HorizontalAlignment="Center" TextWrapping="Wrap" Text="Registrace" VerticalAlignment="Center" FontSize="48" FontFamily="Constantia" TextDecorations="Underline" FontWeight="Bold"/>

        <Label Grid.Column="1" Grid.Row="2" HorizontalAlignment="Left" VerticalAlignment="Top" ClipToBounds="True" Content="Uživatelské jméno:" FontSize="16"/>
        <TextBox Name="UsernameBox" Grid.Column="1" Grid.Row="2" HorizontalAlignment="Stretch" Height="25" TextWrapping="Wrap" Text="" VerticalAlignment="Center" VerticalContentAlignment="Center"/>

        <Label Grid.Column="1" Grid.Row="3" HorizontalAlignment="Left" VerticalAlignment="Top" ClipToBounds="True" Content="Heslo:" FontSize="16"/>
        <PasswordBox Name="PasswordBox" Grid.Column="1" Grid.Row="3" HorizontalAlignment="Stretch" Height="25" VerticalAlignment="Center" Password="" VerticalContentAlignment="Center"/>

        <Button Content="Registrovat"  Grid.Column="1" Grid.Row="4" HorizontalAlignment="Right" Height="25" VerticalAlignment="Top" Width="250" Click="RegistrateToVM" FontSize="16"/>
        <Button Grid.Column="1" Grid.Row="4" Content="Máš účet a chceš se přihlásit?" HorizontalAlignment="Left" Height="25" VerticalAlignment="Top" Width="250" FontSize="16" Command="{Binding ToLoginCommandCommand, Mode=OneWay}" />

    </Grid>
</UserControl>
